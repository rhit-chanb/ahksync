# ahksync
Simple place to put my AutoHotkey scripts and sync them between the devices that use them.

Win+R and run shell:startup to open the startup folder, and make a shortcut in that folder to the file the_script.ahk in this repo in order to make the script run at startup.
